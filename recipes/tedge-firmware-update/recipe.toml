description = "Add workflow scripts to support firmware updates"
priority = 10_000
dependencies = [
    "core/pkg-update",
    "thin-edge.io",
    "health-check",
]

[parameters]
autorollback = { default = true }
