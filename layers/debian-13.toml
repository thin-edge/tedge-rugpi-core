#:schema https://raw.githubusercontent.com/rugix/rugix-bakery/refs/tags/v0.9.0/schemas/rugix-bakery-layer.schema.json
root = true

recipes = [
    "core/debian-bootstrap",

    # Use podman instead of docker as docker isn't available on trixie yet
    "docker",
    "parsec",
]

[parameters."core/debian-bootstrap"]
suite = "trixie"
