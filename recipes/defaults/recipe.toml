description = "thin-edge.io default recipes"
dependencies = [
    "core/raspberrypi",
    "core/persist-root-home",
    "rugpi-extra/zsh",

    # thin-edge.io recipes
    "persist-data",
    "persist-network-manager",
    "persist-overlay",
    "tedge-bootstrap",
    "tedge-firmware-update",
    "x-avahi-daemon",
    "set-wifi",
    "set-wifi-multi",
    "mosquitto",
    "sbom",
    "ssh",
]